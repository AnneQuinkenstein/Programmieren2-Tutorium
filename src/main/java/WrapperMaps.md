Verwendung von Entry in einer HashMap:

    Erstelle eine HashMap mit String als Schlüssel und Integer als Wert.
```Java

``

    Iteriere über die Map-Einträge (Entry) und gib Schlüssel und Wert jedes Eintrags aus.